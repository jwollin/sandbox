{
  "name": "@sandbox/python-api",
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd apps/apis/python && uvicorn main:app --reload --port 8000"
      }
    }
  }
}
