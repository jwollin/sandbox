import { Request, Response, Router } from 'express'; import meta from
'./meta.json'; export const route = Router(); route.get('/', (_req: Request,
res: Response) => { return res.json({ status: 'ok', name:
{{name}}', timestamp: new Date().toISOString(), meta, }); }); export default
route;